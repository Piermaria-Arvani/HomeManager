<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    app:layout_behavior="@string/appbar_scrolling_view_behavior"
    tools:context="it.unitn.disi.homemanager.EventsActivity"
    tools:showIn="@layout/activity_events">


    <TextView
        android:id="@+id/textView"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/eventi_in_programma"
        android:textSize="20sp"
        android:paddingLeft="@dimen/activity_horizontal_margin"
        android:background="#ffb380"/>

    <ListView
        android:layout_below="@+id/textView"
        android:id="@+id/listView"
        android:layout_marginTop="@dimen/activity_horizontal_margin"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content" >
    </ListView>

    <TextView
        android:id="@+id/text_no_eventi"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/non_ci_sono_eventi_in_programma"
        android:layout_marginTop="100sp"
        android:textAlignment="center"
        android:visibility="gone"
        />

</RelativeLayout>
